import React from 'react';
import Navbar from "../components/navbar"

const Home = ({}) => {
    const handleClick = () => {};
    return (
        <div>
            <Navbar/>
            <h1>Create and HOMEPAGE</h1>
        </div>
    );
}

export default Home;
